SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
MAILTO=""

# Runs the Delta position-resolution read-only watchdog every 30 minutes.
# The runner writes timestamped JSON/log artifacts and keeps mutation/apply flags absent.
*/30 * * * * root cd /opt/auralpha/Backend/aurAlpha && /usr/bin/flock -n /var/lock/auralpha-delta-position-resolution-watchdog.lock bash -lc './scripts/checks/run-suggested-trades-delta-position-resolution-watchdog.sh' >> /var/log/auralpha-delta-position-resolution-watchdog.log 2>&1
